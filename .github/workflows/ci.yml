name: Build golddrive

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  workflow_dispatch:

env:
  CACHE_PATH: C:\cache

jobs:
  build-and-test:
    runs-on: windows-2025
    strategy:
      fail-fast: false
      matrix:
        platform: [x64, x86, ARM64]
        configuration: [Release]
        include:
          - platform: x64
            configuration: Release
            run_full_tests: true
          - platform: x86
            configuration: Release
            run_full_tests: false
          - platform: ARM64
            configuration: Release
            run_full_tests: false
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Setup cache directory
        shell: cmd
        run: |
          if not exist C:\cache mkdir C:\cache

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: C:\cache
          key: ${{ runner.os }}-cache-${{ hashFiles('**/lockfiles') }}
          restore-keys: |
            ${{ runner.os }}-cache-

      - name: Extract version
        id: version
        shell: pwsh
        run: |
          $version = python -c "import re; print(re.search(r'<MyVersion>([0-9\.]+)</MyVersion>.+', open('src\\version.xml').read().replace('\n','')).group(1))"
          echo "VERSION=$version" >> $env:GITHUB_OUTPUT
          echo "Version: $version Configuration: ${{ matrix.configuration }} Platform: ${{ matrix.platform }}"

      - name: Setup MSBuild (Visual Studio 2022)
        uses: microsoft/setup-msbuild@v2
        with:
          vs-version: '[17.0,18.0)'

      - name: Install WinFSP (full test builds)
        if: matrix.run_full_tests == true
        shell: cmd
        run: start /wait msiexec /i vendor\winfsp\winfsp-2.1.25156.msi /qn ADDLOCAL=ALL

      - name: Install WinFSP (non-test builds)
        if: matrix.run_full_tests != true
        shell: cmd
        run: start /wait msiexec /i vendor\winfsp\winfsp-2.1.25156.msi /qn ADDLOCAL=ALL

      - name: Setup WSL (full test only)
        if: matrix.run_full_tests == true
        shell: pwsh
        run: |
          wslconfig /u Ubuntu-18.04
          tools\setup_wsl.ps1

      - name: Install Python dependencies (full test only)
        if: matrix.run_full_tests == true
        shell: cmd
        run: python -m pip install --disable-pip-version-check paramiko >NUL

      - name: Setup SSH (full test only)
        if: matrix.run_full_tests == true
        shell: cmd
        run: python tools\setupssh.py support@localhost support

      - name: Build solution
        shell: cmd
        env:
          CONFIGURATION: ${{ matrix.configuration }}
          PLATFORM: ${{ matrix.platform }}
        run: msbuild src\golddrive.sln -p:Configuration=%CONFIGURATION% -p:Platform=%PLATFORM% -p:PlatformToolset=v143 -m /verbosity:quiet

      - name: Check build version
        shell: cmd
        env:
          CONFIGURATION: ${{ matrix.configuration }}
          PLATFORM: ${{ matrix.platform }}
        run: src\.build\%CONFIGURATION%\%PLATFORM%\golddrive.exe --version

      - name: Install MSI
        shell: cmd
        env:
          CONFIGURATION: ${{ matrix.configuration }}
          PLATFORM: ${{ matrix.platform }}
          VERSION: ${{ steps.version.outputs.VERSION }}
        run: start /wait msiexec /i src\.build\%CONFIGURATION%\%PLATFORM%\golddrive-%VERSION%-%PLATFORM%.msi /qn
        # Skip installation test for ARM64 since it can't run on x64 host
        if: matrix.platform != 'ARM64'

      - name: Verify installation (x64)
        if: matrix.platform == 'x64'
        shell: cmd
        run: call "c:\Program Files\Golddrive\golddrive.exe" --version

      - name: Verify installation (x86)
        if: matrix.platform == 'x86'
        shell: cmd
        run: call "c:\Program Files (x86)\Golddrive\golddrive.exe" --version

      - name: Run basic tests
        if: matrix.run_full_tests == true
        shell: cmd
        run: tools\test.bat

      - name: Mount network drive
        if: matrix.run_full_tests == true
        shell: pwsh
        run: |
          net use Z: \\golddrive\support@localhost
          Z:; cd Z:\tmp; echo hello > test.txt; cd

      - name: Run comprehensive tests
        if: matrix.run_full_tests == true
        shell: pwsh
        run: |
          $sw = [Diagnostics.Stopwatch]::StartNew()
          C:\projects\golddrive\vendor\fstools\fsx.exe -N 5000 test xxxxxx
          C:\projects\golddrive\vendor\fstools\fsbench-x64.exe --files=100 -rdwr_cc_* -mmap_* -file_attr* -file_list_single* -file_list_none* -rdwr_nc_*
          C:\projects\golddrive\vendor\iozone\iozone.exe -s100m -r2m
          $sw.Stop(); Write-host "Time: " $sw.Elapsed.totalseconds

      - name: Unmount network drive
        if: matrix.run_full_tests == true && always()
        shell: pwsh
        run: |
          C:; net use Z: /d
        continue-on-error: true

      - name: Upload MSI artifact
        uses: actions/upload-artifact@v4
        with:
          name: golddrive-${{ steps.version.outputs.VERSION }}-${{ matrix.platform }}-msi
          path: src\.build\${{ matrix.configuration }}\${{ matrix.platform }}\golddrive-*.msi
          if-no-files-found: error

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: golddrive-${{ steps.version.outputs.VERSION }}-${{ matrix.platform }}-exe
          path: src\.build\${{ matrix.configuration }}\${{ matrix.platform }}\golddrive-*-${{ matrix.platform }}.exe
          if-no-files-found: error
